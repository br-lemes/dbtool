{
    "autoload": {
        "psr-4": {
            "DBTool\\": "src/"
        }
    },
    "require": {
        "symfony/console": "^7.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^12.0"
    },
    "scripts": {
        "coverage": "docker compose up --wait && phpunit --coverage-html coverage",
        "test": "docker compose up --wait && phpunit"
    }
}
